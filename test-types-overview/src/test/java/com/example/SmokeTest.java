package com.example;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;


@SpringBootTest
public class SmokeTest {

    @Test
    void contextLoads() {
        // If the context loads without errors, this test will pass.
        // This essentially checks if the Spring Boot application can start up.
    }
}